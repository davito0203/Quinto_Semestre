#Actividad Clase 1.
#Medidas de tendencia central: Media, Mediana

x=c(2,5,7,9,12)
x

#Datos no Agrupados

n=length(x)
n

Media=sum(x)/n
Media

summary(x)

stem(x)
stem(x,scale=2)

#Actividad en clase

x1=c(2,5,7,9,125)
x1

#Datos no agrupados

n1=length(x1)
n1

Media1=sum(x1)/n1
Media1
mean(x)
median(x)

summary(x1)

stem(x1)
stem(x1,scale=2)

#Datos

x2=c(18,25,32,2,5,83,92,105,93,150,19,280,110)
x2

#Datos no agrupados

n2=length(x2)
n2

Media=sum(x2)/n2
Media

summary(x2)

stem(x2)
stem(x2,scale=2)